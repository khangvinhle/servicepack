module AssignsHelper
	# This module needs real project_id first, not the slug (Friendly links or whatever)
	# Mainly intended for controllers.
	def assign_to(service_pack, real_project_id)
		
	end
end
